apiVersion: traefik.io/v1alpha1
kind: Middleware
metadata:
  name: {{ .Release.Name }}-body-size
  labels:
    {{- include "nextcloud.labels" . | nindent 4 }}
spec:
  buffering:
    maxRequestBodyBytes: 10737418240  # 10GB

